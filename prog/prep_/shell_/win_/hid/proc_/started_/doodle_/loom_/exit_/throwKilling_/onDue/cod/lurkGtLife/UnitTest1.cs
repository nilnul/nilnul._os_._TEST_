using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Diagnostics;

namespace nilnul._os_._TEST_.prog.prep_.shell_.win_.hid.proc_.started_.due0manual_.loom_.quit_.soft_.onDue.cod.lurkGtLife
{
	[TestClass]
	public class UnitTest1
	{
		[TestMethod]
		public void TestMethod1()
		{
			try
			{
				var c = nilnul.os.prog.prep_.shell_.win_.hid.proc_.started_.doodle_.loom_.exit_.throwKilling_.onDue._CodX._Cod_loomAssumeAftLurk(
					"notepad.exe"
					
					,
					10 * 1000
					,
					30 * 1000
					,
					20 * 1000
					,
					10 * 1000
				);
				/// the window and the request to exit is not shown all time

				Debug.WriteLine(c);


			}
			catch (os.proc_.started.kill_.xpn_.ThouShaltNotKill k)
			{

				Debug.WriteLine("killed");
			}

		}

	}
}
