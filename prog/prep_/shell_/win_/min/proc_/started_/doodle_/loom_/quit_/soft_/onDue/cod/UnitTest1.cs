using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Diagnostics;

namespace nilnul._os_._TEST_.prog.prep_.shell_.win_.min.proc_.started_.invis_.doole_.loom_.quit_.soft_.onDue.cod
{
	[TestClass]
	public class UnitTest1
	{
		[TestMethod]
		public void TestMethod1()
		{

			var c = nilnul.os.prog.prep_.shell_.win_.min.proc_.started_.invis_.defer_.loom_.exit_.throwKilling_.onDue._CodX._ExitCode(
				"notepad.exe"
				,
				(string)null
				,
				null
				,
				15*1000
				,
				35*1000
				,
				20*1000
				,
				10*1000
			);
			/// the window and the request to exit is not shown all time

			Debug.WriteLine(c);

		}

	}
}
